import { Inter, Domine } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], display: 'swap', variable: '--font-inter'});
export const domine = Domine({ subsets: ['latin'], display: 'swap', variable: '--font-domine'});